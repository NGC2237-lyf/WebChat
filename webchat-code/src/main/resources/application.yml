server:
  port: 8080
  servlet:
    context-path: /



spring:
  profiles:
    active: fengyang
    jackson:
      # json 序列化排除值为 null 的属性
      default-property-inclusion: non_null


mybatis:
  mapper-locations: classpath*:guy/mapper/*.xml
  type-aliases-package: tyut.homework.webchat
